package org.acme.getting.started;

import io.quarkus.test.junit.QuarkusTest;

@QuarkusTest
public class GreetingResourceTest {

    /*
     * @Test public void testHelloEndpoint() { given() .when().get("/hello") .then()
     * .statusCode(200) .body(is("Hello RESTEasy")); }
     */
}